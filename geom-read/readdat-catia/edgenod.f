        subroutine edgenod(i,j,k)

        if (i.eq.1) then
        j=1
        k=2
        else
        endif
        if (i.eq.2) then
        j=2
        k=3
        else
        endif
        if (i.eq.3) then
        j=3
        k=1
        else
        endif

        return
        end

